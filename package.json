{
  "name": "fe",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@types/jest": "^26.0.0",
    "@types/node": "^14.0.13",
    "@types/react": "^16.9.0",
    "@types/react-dom": "^16.9.0",
    "firebase-tools": "^8.4.3",
    "prettier": "2.0.5"
  },
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^10.2.1",
    "@testing-library/user-event": "^12.0.6",
    "firebase": "^7.14.4",
    "firebaseui": "^4.5.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.1",
    "typescript": "~3.9.5"
  },
  "scripts": {
    "bootstrap": "yarn --frozen-lockfile --cache-folder ~/.cache/yarn",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "firebase deploy",
    "deploy-ci": "firebase deploy --token ${FIREBASE_TOKEN}",
    "release": "yarn build && yarn deploy",
    "release-ci": "yarn build && yarn deploy"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
