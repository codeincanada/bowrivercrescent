version: 2.1
jobs:
  build:
    docker:
      - image: 'circleci/node:latest'
    steps:
      - restore_cache:
          key: yarn-v1-{{ .Branch }}-{{ checksum "yarn.lock" }}
      - checkout
      - save_cache:
          paths:
            - ~/.cache/yarn
            - ~/usr/local/lib/node_modules
          key: yarn-v1-{{ .Branch }}-{{ checksum "yarn.lock" }}
      - run: yarn
      - run: yarn release-ci
workflows:
  version: 2
  release:
    jobs:
      - build
